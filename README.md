# Typed dYdX

**This repository has moved.**

`typed-dydx` now lives in [`tribulnation/typed`](https://github.com/tribulnation/typed/tree/main/packages/dydx), alongside every other Typed client. This repo is archived and read-only.

```bash
pip install typed-dydx
```
